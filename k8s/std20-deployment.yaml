
apiVersion: apps/v1
kind: Deployment
metadata:
  name: std20-nginx-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: std20-nginx-web
  template:
    metadata:
      labels:
        app: std20-nginx-web
    spec:
      containers:
      - name: std20-nginx-container
        image: smile1101/nginx:1.31.3-cd
        ports:
          - containerPort: 80
--- 


apiVersion: v1
kind: Service
metadata:
  name: std20-nginx-service
spec:
  type: LoadBalancer
  selector:
    app: std20-nginx-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80


---